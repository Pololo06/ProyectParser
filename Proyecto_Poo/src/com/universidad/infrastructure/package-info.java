/** Infrastructure layer (outermost ring): TPA persistence, CLI views,
 * converters, file routes and the composition root. May depend inward on
 * application, interfaceadapters contracts and domain. Nothing depends on it. */
package com.universidad.infrastructure;
